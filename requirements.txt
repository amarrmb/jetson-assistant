# Core runtime dependencies for jetson-assistant voice assistant
# Install with: pip install -r requirements.txt
# Or use extras: pip install -e ".[kokoro,nemotron,assistant,vision]"
#
# System prerequisites (Ubuntu/Debian):
#   sudo apt-get install -y libportaudio2 espeak-ng

# --- Core ---
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=10.0
pydantic>=2.0
numpy>=1.24.0
scipy>=1.10.0
pypdf>=3.0.0
docx2txt>=0.8
typer>=0.9.0
rich>=13.0.0
pyyaml>=6.0

# --- Voice assistant ---
sounddevice>=0.4.6
webrtcvad>=2.0.10
aiohttp>=3.9
openai>=1.0.0
duckduckgo-search>=7.0
cryptography>=41.0

# --- TTS: Kokoro ---
kokoro>=0.9.4
soundfile>=0.12.0
misaki[zh]
misaki[ja]

# --- STT: Nemotron ---
nemo_toolkit[asr]>=2.0.0,<2.6.2
lhotse<1.31
ordered-set>=4.1.0

# --- Vision ---
opencv-python-headless>=4.8.0
